{
  "name": "@services/api-albums",
  "version": "0.0.1",
  "peerDependencies": {
    "@angular/common": "^10.0.2",
    "@angular/core": "^10.0.2",
    "firebase": "^7.13.1",
    "@angular/fire": "^6.0.2"
  },
  "dependencies": {
    "tslib": "^2.0.0"
  },
  "private": true,
  "scripts": {
    "build": "ng build @services/api-albums --prod",
    "watch": "ng build @services/api-albums --watch",
    "test": "ng test @services/api-albums",
    "lint": "ng lint @services/api-albums"
  }
}
