{
  "name": "@services/api-login",
  "version": "0.0.1",
  "peerDependencies": {
    "@angular/common": "^10.0.2",
    "@angular/core": "^10.0.2",
    "@angular/fire": "^6.0.2",
    "firebase": "^7.16.0"
  },
  "dependencies": {
    "tslib": "^2.0.0"
  },
  "private": true,
  "scripts": {
    "build": "ng build @services/api-login --prod",
    "watch": "ng build @services/api-login --watch",
    "test": "ng test @services/api-login",
    "lint": "ng lint @services/api-login"
  }
}
