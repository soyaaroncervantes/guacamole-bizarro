{
  "name": "@routes/albums",
  "version": "0.0.1",
  "peerDependencies": {
    "@angular/common": "^10.0.2",
    "@angular/core": "^10.0.2"
  },
  "dependencies": {
    "tslib": "^2.0.0"
  },
  "private": true,
  "scripts": {
    "build": "ng build @routes/albums --prod",
    "watch": "ng build @routes/albums --watch",
    "test": "ng test @routes/albums",
    "lint": "ng lint @routes/albums"
  }
}
