{
  "name": "@shared/header",
  "version": "0.0.1",
  "peerDependencies": {
    "@angular/common": "^10.0.2",
    "@angular/core": "^10.0.2"
  },
  "dependencies": {
    "tslib": "^2.0.0"
  },
  "private": true,
  "scripts": {
    "build": "ng build @shared/header --prod",
    "watch": "ng build @shared/header --watch",
    "test": "ng test @shared/header",
    "lint": "ng lint @shared/header"
  }
}
